# Overrides for `just check-german`: lift the prompt cap so every open fixture Word
# lands in /learn/german/prompt even when newer real Sessions exist.
german_prompt:
  words: 1000
  minutes: 15
  level: "B1–B2"
